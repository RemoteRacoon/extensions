import {
  initialProperties,
  template,
  definition,
  controller,
  paint,
  resize
} from './extension';

export default {
  initialProperties,
  template,
  definition,
  controller,
  paint,
  resize
};
